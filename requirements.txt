# requirements.txt — MAKE30 dynamic prediction pipeline
#
# CONFIRMED pins (verified from the notebook environment / saved model):
#   numpy, pandas      -> printed by the notebook ("pandas 2.3.3 | numpy 1.24.4")
#   scikit-learn       -> from the model pickle (estimator serialized with 1.3.0)
#   lightgbm           -> trained model reports the v4 format (4.x)
#
# RECOMMENDED pins (not printed anywhere in the notebook; chosen to be
# mutually compatible with numpy 1.24 / scikit-learn 1.3). REPLACE these with
# your actual versions — from your training machine run:
#       pip freeze > requirements.lock.txt
#   or, in the notebook:  import scipy, shap, statsmodels, matplotlib, seaborn, lifelines, joblib, pyarrow
#                         and print each .__version__
# and copy the exact numbers in below. Exact pins matter for bit-for-bit
# reproducibility of the model and metrics (and for review / EB-1A evidence).

# --- core data stack (CONFIRMED) ---
numpy==1.24.4
pandas==2.3.3
scikit-learn==1.3.0
lightgbm>=4.0,<5.0          # CONFIRMED major (v4); set to your exact 4.x, e.g. lightgbm==4.3.0

# --- modeling / stats / interpretability (RECOMMENDED — verify) ---
scipy>=1.10,<1.12
statsmodels>=0.14,<0.15
shap>=0.44,<0.46
lifelines>=0.27,<0.29

# --- io / serialization (RECOMMENDED — verify) ---
joblib>=1.3,<1.4           # keep close to the scikit-learn 1.3 era for clean unpickling
pyarrow>=14.0,<16.0        # required for the parquet caching used by the pipeline

# --- plotting (RECOMMENDED — verify) ---
matplotlib>=3.7,<3.9
seaborn>=0.13,<0.14

# --- notebook execution (optional; only if running the .ipynb directly) ---
# jupyterlab>=4.0
# ipykernel>=6.25
# nbstripout>=0.7          # used to clear notebook outputs before committing
